/** Output format toggle: Jira Cloud (ADF) vs. Jira Server/Data Center (Wiki Markup). */
export type OutputFormat = 'wiki' | 'adf'

/** View mode toggle: rendered preview vs. raw code. */
export type ViewMode = 'preview' | 'code'
