/** Output format toggle: Jira Cloud (ADF), Jira Server/Data Center (Wiki Markup), or Confluence Storage Format. */
export type OutputFormat = 'wiki' | 'adf' | 'confluence'

/** View mode toggle: rendered preview vs. raw code. */
export type ViewMode = 'preview' | 'code'
